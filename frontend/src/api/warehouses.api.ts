import { api } from './axios'; import type { Almacen, CreateAlmacenRequest } from '../types/warehouse'
export const warehousesApi = { list: () => api.get<Almacen[]>('/warehouses').then(r => r.data), byBranch: (idSucursal: number) => api.get<Almacen[]>(`/warehouses/sucursal/${idSucursal}`).then(r => r.data), get: (id: number) => api.get<Almacen>(`/warehouses/${id}`).then(r => r.data), create: (body: CreateAlmacenRequest) => api.post<Almacen>('/warehouses', body).then(r => r.data), update: (id: number, body: Partial<CreateAlmacenRequest>) => api.patch<Almacen>(`/warehouses/${id}`, body).then(r => r.data), remove: (id: number) => api.delete(`/warehouses/${id}`).then(r => r.data) }
