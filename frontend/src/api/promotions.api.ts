import { api } from './axios'
export interface PromocionRequest { nombre: string; porcentaje: number; fechaInicio?: string; fechaFin?: string; estado?: boolean; idsProductos?: number[]; idsCategorias?: number[]; idsTemporadas?: number[] }
export const promotionsApi = { list: () => api.get('/promotions').then(r => r.data), active: () => api.get('/promotions/activas').then(r => r.data), byProduct: (id: number) => api.get(`/promotions/producto/${id}`).then(r => r.data), get: (id: number) => api.get(`/promotions/${id}`).then(r => r.data), create: (body: PromocionRequest) => api.post('/promotions', body).then(r => r.data), update: (id: number, body: Partial<PromocionRequest>) => api.patch(`/promotions/${id}`, body).then(r => r.data), remove: (id: number) => api.delete(`/promotions/${id}`).then(r => r.data) }
