import { api } from './axios'; import type { AssignBranchRequest, CreateUserRequest, User } from '../types/user'
export const usersApi = { list: () => api.get<User[]>('/users').then(r => r.data), get: (id: number) => api.get<User>(`/users/${id}`).then(r => r.data), create: (body: CreateUserRequest) => api.post<User>('/users', body).then(r => r.data), assignBranch: (id: number, body: AssignBranchRequest) => api.post(`/users/${id}/sucursales`, body).then(r => r.data), branches: (id: number) => api.get(`/users/${id}/sucursales`).then(r => r.data), deactivateBranch: (id: number, branchId: number) => api.delete(`/users/${id}/sucursales/${branchId}`).then(r => r.data) }
