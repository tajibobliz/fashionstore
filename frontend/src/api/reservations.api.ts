import { api } from './axios'; import type { CreateReservaRequest, Reserva, ReservaEstado } from '../types/reservation'
export const reservationsApi = { list: () => api.get<Reserva[]>('/reservations').then(r => r.data), mine: () => api.get<Reserva[]>('/reservations/me').then(r => r.data), get: (id: number) => api.get<Reserva>(`/reservations/${id}`).then(r => r.data), create: (body: CreateReservaRequest) => api.post<Reserva>('/reservations', body).then(r => r.data), updateStatus: (id: number, estado: ReservaEstado) => api.patch<Reserva>(`/reservations/${id}/estado`, { estado }).then(r => r.data), cancel: (id: number) => api.patch<Reserva>(`/reservations/${id}/cancel`).then(r => r.data) }
