import { api } from './axios'; import type { CreateVentaCarritoRequest, CreateVentaPresencialRequest, Venta } from '../types/sale'
export const salesApi = { fromCart: (body: CreateVentaCarritoRequest) => api.post<Venta>('/sales/from-cart', body).then(r => r.data), presencial: (body: CreateVentaPresencialRequest) => api.post<Venta>('/sales/presencial', body).then(r => r.data), mine: () => api.get<Venta[]>('/sales/me').then(r => r.data), list: () => api.get<Venta[]>('/sales').then(r => r.data), get: (id: number) => api.get<Venta>(`/sales/${id}`).then(r => r.data), confirm: (id: number) => api.patch<Venta>(`/sales/${id}/confirmar`).then(r => r.data), cancel: (id: number) => api.patch<Venta>(`/sales/${id}/cancelar`).then(r => r.data) }
